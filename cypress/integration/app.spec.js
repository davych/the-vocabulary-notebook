/// <reference types="Cypress" />

describe('Given a user open our application', () => {
  beforeEach(() => {
    cy.visitIndex()
  })

  it('should display the website without error', () => {

  })
})
